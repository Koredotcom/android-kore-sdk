<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Non-progress part -->
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="@color/greyHead" />
        </shape>
    </item>
    <!-- Progress part -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/greyHead" />
            </shape>
        </clip>
    </item>
</layer-list>